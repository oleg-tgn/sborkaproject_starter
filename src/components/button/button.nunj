{% macro button(props) %}
<button class="button{% if props.className %} {{ props.className }}{% endif %}" {% if props.type %} type="{{ props.type }}"{% endif %}>
	{{ props.text }}	
</button>
{% endmacro %}